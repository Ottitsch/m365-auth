# Local gateway auth. Optional, but recommended if anything besides your user
# can reach the gateway process.
M365_PROXY_API_KEY=change-me

# The remaining values are generated from your own HAR and must stay private.
# Run:
#   python -m m365auth --init-env
#
# Do not put real values in this example file.
